name: fc_quick_dialog
description: "Quick and simple material-based dialogs like JavaScript's `alert`, `confirm` and `prompt`."
version: 0.2.0
homepage: https://github.com/flutter-cavalry/fc_quick_dialog

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

platforms:
  android:
  ios:
  macos:
  windows:
  linux:

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
